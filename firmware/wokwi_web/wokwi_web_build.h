// Маркер сборки в веб-редакторе Wokwi. Этот файл кладётся в корень
// веб-проекта (https://wokwi.com/projects/475304919575815169) и только туда.
// В репозитории он лежит в подкаталоге, чтобы не попадать в локальную сборку.
//
// Веб-редактор Wokwi собирает скетч с голым FQBN esp32:esp32:esp32s3: без
// PSRAM и с библиотеками IDF под quad-PSRAM (wokwi/wokwi-features#809).
// Пока этот файл виден из board_config.h, такая сборка не останавливается
// на #error, но при старте пишет в лог предупреждение о нехватке памяти.
#pragma once
